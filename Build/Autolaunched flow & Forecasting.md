Setup -> Flows



Autolaunched flow -> no event to trigger it is subflow that automatically work like email is sent automatically

Screen low -> nothing to trigger just screen flow

Record flow -> the condition will be set like the price is greated than 5 lakh or if user selected laptop then action might taken

scheduled flow -> based on time the trigger is given





**Forecasting**



when the opportunity is closed then it is added to revenue. For 2 months we can set quota and we can observe the opportunity converted if it exceeds quota we can scale up our business this is called forecasting

